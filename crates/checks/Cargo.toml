[package]
name = "vpnhide_checks"
version = "1.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Native VPN visibility checks shared by the app JNI and root helper."

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
