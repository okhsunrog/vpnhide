_2026-09-15_

## English

Configuration switches now show the requested value and progress immediately, without moving the screen while saving; errors appear as overlays with recovery actions. App edits survive Activity recreation, concurrent writes preserve unrelated fields, and root operations with an unknown outcome pause safely after one automatic recheck.

## Русский

Переключатели конфигурации сразу показывают выбранное положение и прогресс и больше не сдвигают экран при сохранении; ошибки показываются поверх экрана с действиями восстановления. Правки приложений переживают пересоздание Activity, параллельные записи сохраняют несвязанные поля, а операции root с неизвестным результатом безопасно приостанавливаются после одной автоматической перепроверки.
