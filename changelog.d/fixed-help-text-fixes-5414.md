_2026-09-10_

## English

Corrected in-app help text: NetworkInterface is covered by the native backend (not Java), ports rules apply on Save (only Zygisk needs an app restart), the hidden-apps list is under "VPN app hiding", and VPN Hide keeps one native selection rather than writing to every backend

## Русский

Исправлены тексты справки в приложении: NetworkInterface закрывает Native-бэкенд (а не Java), правила портов применяются при сохранении (перезапуск приложения нужен только для Zygisk), список скрытых приложений находится в «Скрытии VPN-приложений», а VPN Hide хранит один выбор Native, а не пишет во все бэкенды
