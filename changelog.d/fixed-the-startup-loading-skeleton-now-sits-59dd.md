_2026-09-15_

## English

The startup loading skeleton now sits inside the real screen with its header and tab bar, so the layout no longer shifts down once the dashboard has loaded.

## Русский

Скелет загрузки при старте теперь показывается внутри настоящего экрана с его шапкой и вкладками, поэтому после загрузки панели содержимое больше не сдвигается вниз.
