diff --git a/fs/stat.c b/fs/stat.c
index 517fa4ed8..90eb3681d 100644
--- a/fs/stat.c
+++ b/fs/stat.c
@@ -22,6 +22,7 @@
 
 #include <linux/uaccess.h>
 #include <asm/unistd.h>
+#include <linux/vpnhide.h>
 
 #include "internal.h"
 #include "mount.h"
@@ -173,7 +174,10 @@ int vfs_getattr(const struct path *path, struct kstat *stat,
 	retval = security_inode_getattr(path);
 	if (retval)
 		return retval;
-	return vfs_getattr_nosec(path, stat, request_mask, query_flags);
+	retval = vfs_getattr_nosec(path, stat, request_mask, query_flags);
+	if (!retval && vpnhide_should_hide_dentry(path->dentry))
+		retval = -ENOENT;
+	return retval;
 }
 EXPORT_SYMBOL_NS(vfs_getattr, ANDROID_GKI_VFS_EXPORT_ONLY);
 
