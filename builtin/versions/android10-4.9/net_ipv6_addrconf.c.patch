diff --git a/net/ipv6/addrconf.c b/net/ipv6/addrconf.c
index 2c28cd5f4..5dc90c985 100644
--- a/net/ipv6/addrconf.c
+++ b/net/ipv6/addrconf.c
@@ -77,6 +77,7 @@
 #include <net/ndisc.h>
 #include <net/ip6_route.h>
 #include <net/addrconf.h>
+#include <linux/vpnhide.h>
 #include <net/tcp.h>
 #include <net/ip.h>
 #include <net/netlink.h>
@@ -4628,6 +4629,11 @@ static int inet6_fill_ifaddr(struct sk_buff *skb, struct inet6_ifaddr *ifa,
 	struct nlmsghdr  *nlh;
 	u32 preferred, valid;
 
+	if (ifa->idev && ifa->idev->dev &&
+	    vpnhide_should_hide_dev(ifa->idev->dev,
+				    VPNHIDE_HID_INET6_FILL_IFADDR))
+		return 0;
+
 	nlh = nlmsg_put(skb, portid, seq, event, sizeof(struct ifaddrmsg), flags);
 	if (!nlh)
 		return -EMSGSIZE;
