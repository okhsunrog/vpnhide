diff --git a/net/ipv6/addrconf.c b/net/ipv6/addrconf.c
index 39be4b4c7..962889021 100644
--- a/net/ipv6/addrconf.c
+++ b/net/ipv6/addrconf.c
@@ -74,6 +74,7 @@
 #include <net/ndisc.h>
 #include <net/ip6_route.h>
 #include <net/addrconf.h>
+#include <linux/vpnhide.h>
 #include <net/tcp.h>
 #include <net/ip.h>
 #include <net/netlink.h>
@@ -5056,6 +5057,11 @@ static int inet6_fill_ifaddr(struct sk_buff *skb, struct inet6_ifaddr *ifa,
 	struct nlmsghdr  *nlh;
 	u32 preferred, valid;
 
+	if (ifa->idev && ifa->idev->dev &&
+	    vpnhide_should_hide_dev(ifa->idev->dev,
+				    VPNHIDE_HID_INET6_FILL_IFADDR))
+		return 0;
+
 	nlh = nlmsg_put(skb, args->portid, args->seq, args->event,
 			sizeof(struct ifaddrmsg), args->flags);
 	if (!nlh)
